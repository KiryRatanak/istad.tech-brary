#pragma once
#include <iostream>
#include <string>
#include <string>
#include <vector>

#include "core/User.hpp"

using namespace std;

struct Book {
    string title;
    string author;
    int year;
    string country;
};

struct History {
    string title;
};